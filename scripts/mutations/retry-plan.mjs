// Mutation set for src/lib/upload/retry-plan.ts -- run with: node scripts/mutations/run.mjs retry-plan
export default {
  file: 'src/lib/upload/retry-plan.ts',
  test: 'tests/retry-plan.test.ts',
  mutations: [
  { name: 'a file whose row is already queued is RE-UPLOADED (the duplicate photo returns)',
    from: "  return pendingSaveIds.has(entryId) ? 'resave' : 'reupload'", to: "  return 'reupload'" },
  { name: 'every retry re-saves, so a genuinely failed upload is never sent again',
    from: "  return pendingSaveIds.has(entryId) ? 'resave' : 'reupload'", to: "  return 'resave'" },
  { name: 'a second network failure parks again instead of handing over the button',
    from: "  return recoverable && !entry.autoResumed", to: "  return recoverable" },
  { name: 'nothing ever parks',
    from: "  return recoverable && !entry.autoResumed", to: "  return false" },
  { name: 'tapping a parked tile earns a fresh resume (it can park forever)',
    from: "    autoResumed: trigger === 'auto' || (trigger === 'tap' && entry.status === 'waiting'),", to: "    autoResumed: trigger === 'auto'," },
  { name: 'every retry spends the resume, so the retry the guest was invited to make can never park',
    from: "    autoResumed: trigger === 'auto' || (trigger === 'tap' && entry.status === 'waiting'),", to: "    autoResumed: true," },
  { name: 'the chip collects parked files too (a tile that is about to upload is listed as failed)',
    from: "  if (trigger === 'chip' && entry.status !== 'error') return null\n", to: "" },
  { name: 'the automatic resume acts on failures as well as parked files',
    from: "  if (trigger === 'auto' && entry.status !== 'waiting') return null\n", to: "" },
  { name: 'a tap retries anything, including a file already uploading',
    from: "  if (trigger === 'tap' && entry.status !== 'error' && entry.status !== 'waiting') return null\n", to: "" },
  { name: 'the retry keeps the old progress and error text',
    from: "    status: 'pending' as FileStatus,\n    progress: 0,\n    error: undefined,", to: "    status: 'pending' as FileStatus," },
  { name: 'a full album always offers an account, even to someone who has one',
    from: "  return nudge === 'register' ? 'full' : 'fullOther'", to: "  return 'full'" },
  { name: 'a full album never offers an account',
    from: "  return nudge === 'register' ? 'full' : 'fullOther'", to: "  return 'fullOther'" },
  { name: 'any refusal is treated as a full album',
    from: "  if (code !== 'album_full') return 'failed'\n", to: "" },
  { name: 'a later transient failure demotes the cap banner',
    from: "  if (prev === 'full') return 'full'\n", to: "" },
  { name: 'fullOther is demoted by a transient failure',
    from: "  if (prev === 'fullOther' && next === 'failed') return 'fullOther'\n", to: "" },
  { name: 'a re-refused file moves to the end of the queue instead of holding its place',
    from: "  const byId = new Map(existing.map((p) => [p.entryId, p]))\n  for (const p of incoming) byId.set(p.entryId, p)\n  return [...byId.values()]",
    to: "  const ids = new Set(incoming.map((p) => p.entryId))\n  return [...existing.filter((x) => !ids.has(x.entryId)), ...incoming]" },
  { name: 'the queue is grown IN PLACE (a save in flight sees rows it never sent)',
    from: "  const byId = new Map(existing.map((p) => [p.entryId, p]))\n  for (const p of incoming) byId.set(p.entryId, p)\n  return [...byId.values()]",
    to: "  for (const p of incoming) {\n    const at = existing.findIndex((x) => x.entryId === p.entryId)\n    if (at >= 0) existing[at] = p\n    else existing.push(p)\n  }\n  return existing" },
  { name: 'a file can wait twice (the banner counts the same photo twice and both rows are posted)',
    from: "  const byId = new Map(existing.map((p) => [p.entryId, p]))\n  for (const p of incoming) byId.set(p.entryId, p)\n  return [...byId.values()]",
    to: "  return [...existing, ...incoming]" },
  // ── what the banner says when nothing is held (2026-09-12) ───────────────────────────────────
  { name: 'A WALL WITH NOTHING HELD CLAIMS PHOTOS ARE UPLOADED, and offers to finish saving an empty queue',
    from: "  const canFinish = heldRows > 0", to: "  const canFinish = true" },
  { name: 'a wall with rows held stops offering to finish saving them',
    from: "  const canFinish = heldRows > 0", to: "  const canFinish = false" },
  { name: 'the no-rows body is used even when rows ARE held',
    from: "body: canFinish ? 'uploadWall.body' : 'uploadWall.bodyNone'", to: "body: 'uploadWall.bodyNone'" },
  { name: 'the held-rows body is used when nothing is held',
    from: "body: canFinish ? 'uploadWall.body' : 'uploadWall.bodyNone'", to: "body: 'uploadWall.body'" },
  { name: 'the other full wall loses its own no-rows body',
    from: "body: canFinish ? 'uploadWall.fullBody' : 'uploadWall.fullBodyNone'", to: "body: 'uploadWall.fullBody'" },
  { name: 'an account is offered on a wall the server never said registering would help',
    from: "  if (wall === 'failed') return { title: 'uploadWall.failedTitle', body: 'uploadWall.failedBody', offersAccount: false, canFinish }",
    to: "  if (wall === 'failed') return { title: 'uploadWall.failedTitle', body: 'uploadWall.failedBody', offersAccount: true, canFinish }" },
  { name: 'the full-album wall stops offering the account it exists for',
    from: "    return { title: 'uploadWall.title', body: canFinish ? 'uploadWall.body' : 'uploadWall.bodyNone', offersAccount: true, canFinish }",
    to: "    return { title: 'uploadWall.title', body: canFinish ? 'uploadWall.body' : 'uploadWall.bodyNone', offersAccount: false, canFinish }" },

  { name: 'A WALL WITH NOTHING HELD SURVIVES THE NEXT ATTEMPT, so it sits over photos that uploaded fine',
    from: "  return heldRows > 0 ? prev : null", to: "  return prev" },
  { name: 'every new attempt clears the wall, including one whose rows are still waiting',
    from: "  return heldRows > 0 ? prev : null", to: "  return null" },
  ],
}
