// Mutation set for src/lib/upload/retry.ts -- run with: node scripts/mutations/run.mjs retry
// Each entry is a change that would make the code WRONG; the tests in tests/retry.test.ts must fail on it.
export default {
  file: 'src/lib/upload/retry.ts',
  test: 'tests/retry.test.ts',
  mutations: [
  // fetchWithRetry
  { name: 'a 5xx is never retried',
    from: "        if (verdict === 'retry') {", to: "        if (false) {" },
  { name: 'server errors are not counted (unbounded 5xx retries)',
    from: "          serverErrors++\n", to: '' },
  { name: 'MAX_SERVER_ERROR_ATTEMPTS is 40',
    from: "export const MAX_SERVER_ERROR_ATTEMPTS = 4", to: "export const MAX_SERVER_ERROR_ATTEMPTS = 40" },
  { name: 'a superseded 5xx body is not drained',
    from: "          void lastServerRes?.body?.cancel()\n          lastServerRes = res", to: "          lastServerRes = res" },
  { name: 'no immediate retry after a confirmed recovery',
    from: "            skipBackoff = true\n", to: '' },
  { name: 'no grace after a confirmed recovery',
    from: "            deadline.extendTo(POST_RECOVERY_GRACE_MS)\n", to: '' },
  { name: 'graces are unlimited (flapping network spins forever)',
    from: "          if (recovered && graces < MAX_RECOVERY_GRACES) {", to: "          if (recovered) {" },
  { name: 'a cancel is retried like a transient failure',
    from: "        if (throwVerdict === 'give-up' && opts.signal?.aborted) {", to: "        if (false) {" },
  { name: 'every throw is treated as network-class (probe on unrelated errors)',
    from: "        if (lastWasNetwork) {", to: "        if (true) {" },
  { name: 'the caller signal is not wired into the attempt (cancel waits for the attempt timeout)',
    from: "      const attemptSignal = withTimeoutSignal(opts.signal, FETCH_ATTEMPT_TIMEOUT_MS)", to: "      const attemptSignal = withTimeoutSignal(undefined, FETCH_ATTEMPT_TIMEOUT_MS)" },
  { name: 'per-attempt timeout is 200s',
    from: "export const FETCH_ATTEMPT_TIMEOUT_MS = 20_000", to: "export const FETCH_ATTEMPT_TIMEOUT_MS = 200_000" },
  { name: 'per-attempt cleanup never runs',
    from: "        attemptSignal.cleanup()\n", to: '' },
  { name: 'out of time with a retained 5xx throws instead of returning it',
    from: "    if (lastServerRes && !lastWasNetwork) return lastServerRes\n", to: '' },
  { name: 'A RECOVERED NETWORK IS STILL CALLED DOWN: the flag is never cleared by a response, so a fresh 5xx is discarded',
    from: "          lastWasNetwork = false\n", to: '' },
  { name: 'A STALE 5xx IS HANDED BACK AFTER THE NETWORK DIED, so the photo is never parked',
    from: "    if (lastServerRes && !lastWasNetwork) return lastServerRes", to: "    if (lastServerRes) return lastServerRes" },
  { name: 'the superseded 5xx is not drained, so its connection stays pinned',
    from: "    void lastServerRes?.body?.cancel()\n    // Name the endpoint", to: "    // Name the endpoint" },
  { name: 'the endpoint is not named in the final error',
    from: "    const err = new Error(`${lastErr?.message ?? 'Network request failed'} (${path})`)", to: "    const err = new Error(`${lastErr?.message ?? 'Network request failed'}`)" },
  { name: 'the final error names the full URL (query and all) instead of the path',
    from: "return new URL(url, 'http://origin.invalid').pathname", to: "return url" },
  { name: 'waitedMs is not attached',
    from: "{ waitedMs: elapsedSince(startedAt), unreachable: lastWasNetwork }", to: "{ unreachable: lastWasNetwork }" },
  // withPutPolicy
  { name: 'a 5xx from R2 is treated as final',
    from: "        if (e instanceof HttpError && e.status < 500) throw e", to: "        if (e instanceof HttpError) throw e" },
  { name: 'a 4xx from R2 is retried',
    from: "        if (e instanceof HttpError && e.status < 500) throw e\n", to: '' },
  { name: 'the probe runs even when the server answered',
    from: "        if (!(e instanceof HttpError)) {\n          await deps.reachability.awaitRecovery", to: "        {\n          await deps.reachability.awaitRecovery" },
  { name: 'an already-aborted signal still attempts',
    from: "      if (signal?.aborted) throw new DOMException('Upload aborted', 'AbortError')\n      if (attempt > 0) {", to: "      if (attempt > 0) {" },
  { name: 'PUT budget is a fixed five attempts again',
    from: "        if (deadline.wouldOverrun(wait)) break\n        await sleep(wait)\n      }\n      attempt++\n      try {\n        return await attemptOnce()", to: "        if (deadline.wouldOverrun(wait) || attempt >= 5) break\n        await sleep(wait)\n      }\n      attempt++\n      try {\n        return await attemptOnce()" },
  // relayUploadImage
  { name: 'isThumb is always sent as 1',
    from: "&isThumb=${target.isThumb ? '1' : '0'}", to: "&isThumb=1" },
  // putImageWithRelay
  { name: 'the direct attempt is never skipped, even when the block is believed',
    from: "    if (!deps.relayPolicy.shouldRelayFirst()) {", to: "    if (true) {" },
  { name: 'a direct HttpError falls through to the relay',
    from: "        if (e instanceof DOMException && e.name === 'AbortError') throw e\n        if (e instanceof HttpError) throw e\n        directFailed = true", to: "        if (e instanceof DOMException && e.name === 'AbortError') throw e\n        directFailed = true" },
  { name: 'the relay result is discarded and the presign-time key returned (the orphan bug)',
    from: "      return result\n    } catch (e) {", to: "      return original\n    } catch (e) {" },
  { name: 'the relay proof is never recorded',
    from: "        deps.relayPolicy.recordRelaySucceededAfterDirectFailure()\n", to: '' },
  { name: 'the relay switch is reported on every relay success, even relay-first',
    from: "      if (directFailed && !deps.relayPolicy.isRelayBelieved()) {", to: "      if (true) {" },
  { name: 'directCause never says skipped',
    from: "directCause: directFailed ? cause(lastDirectErr) : 'skipped'", to: "directCause: cause(lastDirectErr)" },
  // withTimeoutSignal
  { name: 'withTimeoutSignal ignores an already-aborted caller',
    from: "    if (caller.aborted) ctrl.abort(caller.reason)\n    else caller.addEventListener", to: "    caller.addEventListener" },
  { name: 'withTimeoutSignal cleanup leaves the timer',
    from: "      clearTimeout(timer)\n      caller?.removeEventListener", to: "      caller?.removeEventListener" },
  { name: 'withTimeoutSignal cleanup leaves the caller listener',
    from: "      clearTimeout(timer)\n      caller?.removeEventListener('abort', onCallerAbort)\n", to: "      clearTimeout(timer)\n" },
  { name: 'timeout aborts as a plain AbortError (isNetworkClass would say not-network)',
    from: "    ctrl.abort(new DOMException('Timed out', 'TimeoutError'))", to: "    ctrl.abort(new DOMException('Timed out', 'AbortError'))" },
  { name: 'a timed-out attempt is renamed as a plain abort -- the iPhone defect, reintroduced by our own hand',
    from: "? new DOMException('Timed out', 'TimeoutError') : e", to: "? new DOMException('Timed out', 'AbortError') : e" },
  // Added 2026-09-06 after a review's own mutations survived (each named the test that could not see it).
  { name: 'the post-recovery backoff skip is never reset (tight spin once the graces are spent)',
    from: "      skipBackoff = false\n      attempt++", to: "      attempt++" },
  { name: 'fetchWithRetry sleeps past the deadline, then fails on the far side',
    from: "        if (deadline.wouldOverrun(wait)) break\n        await sleep(wait)\n      }\n      skipBackoff = false",
    to:   "        if (deadline.expired()) break\n        await sleep(wait)\n      }\n      skipBackoff = false" },
  { name: 'the first PUT retry has no backoff',
    from: "      if (signal?.aborted) throw new DOMException('Upload aborted', 'AbortError')\n      if (attempt > 0) {",
    to:   "      if (signal?.aborted) throw new DOMException('Upload aborted', 'AbortError')\n      if (attempt > 1) {" },
  { name: 'a relay HttpError is wrapped as a network failure',
    from: "      if (e instanceof HttpError) throw e\n      // Both routes failed on a pure network-level basis.",
    to:   "      // Both routes failed on a pure network-level basis." },
  { name: 'a relay-leg cancel is wrapped as a network failure',
    from: "      if (e instanceof DOMException && e.name === 'AbortError') throw e\n      if (e instanceof HttpError) throw e\n      // Both routes failed on a pure network-level basis.",
    to:   "      if (e instanceof HttpError) throw e\n      // Both routes failed on a pure network-level basis." },

  // ── our timer, whatever the browser calls it (2026-09-11, an iPhone) ───────────────────────────
  { name: "THE BROWSER'S WORDING DECIDES AGAIN: a timeout Safari calls an abort never waits for the connection",
    from: "        const failure = attemptSignal.timedOut() ? new DOMException('Timed out', 'TimeoutError') : e", to: "        const failure = e" },
  { name: 'the verdict is never recorded, so a timed-out attempt neither waits nor parks',
    from: "        lastWasNetwork = isNetworkClass(failure)", to: "        lastWasNetwork = false" },
  { name: 'the wait for the connection keys on the raw error again, not on what happened',
    from: "        if (lastWasNetwork) {", to: "        if (isNetworkClass(e)) {" },
  { name: 'unreachable is not handed on, so the park decision cannot see it',
    from: "{ waitedMs: elapsedSince(startedAt), unreachable: lastWasNetwork }", to: "{ waitedMs: elapsedSince(startedAt) }" },
  { name: 'unreachable is always true, so a failure of another kind parks too',
    from: "{ waitedMs: elapsedSince(startedAt), unreachable: lastWasNetwork }", to: "{ waitedMs: elapsedSince(startedAt), unreachable: true }" },
  { name: "a caller's cancel that landed first is later called a timeout",
    from: "    if (!ctrl.signal.aborted) timedOut = true", to: "    timedOut = true" },
  { name: 'the timer never says it fired',
    from: "    timedOut: () => timedOut,", to: "    timedOut: () => false," },
  ],
}
