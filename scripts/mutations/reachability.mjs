// Mutation set for src/lib/upload/reachability.ts -- run with: node scripts/mutations/run.mjs reachability
// Each entry is a change that would make the code WRONG; the tests in tests/reachability.test.ts must fail on it.
export default {
  file: 'src/lib/upload/reachability.ts',
  test: 'tests/reachability.test.ts',
  mutations: [
  { name: 'a 500 counts as reachable (boundary)',
    from: "      return res.status < 500", to: "      return res.status <= 500" },
  { name: 'any status counts as reachable',
    from: "      return res.status < 500", to: "      return res.status < 600" },
  { name: 'probes even when the browser says offline',
    from: "    if (!online()) return false\n", to: '' },
  { name: 'probe is a GET, not a HEAD',
    from: "{ method: 'HEAD', cache: 'no-store'", to: "{ method: 'GET', cache: 'no-store'" },
  { name: 'probe may be answered from cache',
    from: "{ method: 'HEAD', cache: 'no-store', signal", to: "{ method: 'HEAD', signal" },
  { name: 'probe has no timeout of its own',
    from: ", signal: AbortSignal.timeout(5000) })", to: " })" },
  { name: 'every caller starts its OWN loop (no sharing)',
    from: "    if (!probe) {", to: "    if (true) {" },
  { name: 'the shared slot is never cleared (second outage reuses a settled promise)',
    from: "      void loop.finally(() => { probe = null })\n", to: '' },
  { name: 'never gives up (deadline ignored)',
    from: "          if (deadline.wouldOverrun(wait)) return false\n", to: '' },
  { name: 'budget is 40 minutes, not 4',
    from: "export const REACHABILITY_PROBE_MAX_MS = 4 * 60_000", to: "export const REACHABILITY_PROBE_MAX_MS = 40 * 60_000" },
  { name: 'no jitter -- every phone re-probes in lockstep',
    from: "* (0.5 + random() * 0.5)", to: "* 1" },
  { name: 'no exponential growth (constant 1s)',
    from: "Math.min(5000, 1000 * attempt)", to: "Math.min(5000, 1000)" },
  { name: 'no cap on the wait (grows forever)',
    from: "Math.min(5000, 1000 * attempt)", to: "(1000 * attempt)" },
  { name: 'awaitRecovery probes even with no budget or when already cancelled',
    from: "    if (remainingMs <= 0 || signal?.aborted) return Promise.resolve(false)\n", to: '' },
  { name: 'awaitRecovery never times out (waits for the probe however long)',
    from: "      const timer = setTimeout(() => settle(false), remainingMs)", to: "      const timer = setTimeout(() => settle(false), remainingMs * 1000)" },
  { name: 'awaitRecovery ignores the caller cancel',
    from: "      signal?.addEventListener('abort', onAbort, { once: true })\n", to: '' },
  { name: 'awaitRecovery leaves its budget timer running after the probe wins',
    from: "        clearTimeout(timer)\n        signal?.removeEventListener('abort', onAbort)", to: "        signal?.removeEventListener('abort', onAbort)" },
  ],
}
