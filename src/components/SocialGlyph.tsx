import type { SocialNetwork } from '@/lib/social-profiles'

// The two network marks, drawn once.
//
// lucide-react ships no brand icons (checked against the installed 1.21.0: no Instagram export),
// which is why /about had inlined these paths itself. The footer needs the same two, and a second
// copy of an SVG path is the kind of duplicate nobody diffs.
//
// NO 'use client' AND NO HOOKS, deliberately: /about is a server component and SiteFooter is a
// client one, and a plain component like this renders correctly inside both.

type Props = { network: SocialNetwork; size?: number }

export default function SocialGlyph({ network, size = 16 }: Props) {
  if (network === 'instagram') {
    return (
      <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true" focusable="false">
        <rect width="20" height="20" x="2" y="2" rx="5" ry="5" />
        <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" />
        <line x1="17.5" x2="17.51" y1="6.5" y2="6.5" />
      </svg>
    )
  }
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" focusable="false">
      <path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z" />
    </svg>
  )
}
